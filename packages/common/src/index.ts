// Common utilities and types for SSHR platform
export interface User {
  id: string;
  name: string;
  email: string;
}

export interface Patient {
  id: string;
  name: string;
  dateOfBirth: string;
  phoneNumber: string;
}

// Add more common types and utilities as needed 